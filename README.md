### BASIC CLI APP

#### Requirements

Ensure that Typescript is installed.

npm i -g typescript

#### Steps

> clone repo

> cd into repo dir.

> run \$> `npm i`

> run \$> `tsc`

> run \$> `node dist/index.js`
